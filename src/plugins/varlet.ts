import { Button, Input, Row, Col, Icon, Image, BackTop, Space, Sticky } from '@varlet/ui'
import '@varlet/ui/es/style.js'
export default (App: any) => {
    App.use(Button).use(Input).use(Row).use(Col).use(Icon).use(Image).use(BackTop).use(Space).use(Sticky)
}