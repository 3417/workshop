import { Button,Row, Col, Icon, Image, BackTop, Space } from '@varlet/ui'
import '@varlet/ui/es/style.js'
export default (App: any) => {
    App.use(Button).use(Row).use(Col).use(Icon).use(Image).use(BackTop).use(Space)
}